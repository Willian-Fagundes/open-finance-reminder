version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.11
  command: gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
  network:
    port: 8080
    env: PORT